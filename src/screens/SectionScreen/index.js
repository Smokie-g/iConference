import { connect } from "react-redux";
import SectionScreenComponent from "./SectionScreen";

const mapStateToProps = state => ({});

const mapDispatchToProps = {};

export const SectionScreen = connect(
  mapStateToProps,
  mapDispatchToProps,
)(SectionScreenComponent);