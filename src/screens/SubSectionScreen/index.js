import { connect } from "react-redux";
import SubSectionScreenComponent from "./SubSectionScreen";

const mapStateToProps = state => ({});

const mapDispatchToProps = {};

export const SubSectionScreen = connect(
  mapStateToProps,
  mapDispatchToProps,
)(SubSectionScreenComponent);